# Immagine Node con Alpine Linux per un'immagine più leggera
FROM node:24-alpine

# Cartella di lavoro dentro il container
WORKDIR /app

# Copia package.json e package-lock.json
COPY frontend/orders_manager/package*.json ./

# Installa le dipendenze
RUN npm install

# Copia tutto il frontend
COPY frontend/orders_manager ./

# Espone la porta di Vite (default 5173)
EXPOSE 5173

# Comando per partire il dev server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
